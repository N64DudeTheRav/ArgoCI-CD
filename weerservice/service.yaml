apiVersion: v1
kind: Service
metadata:
  name: weerservice
  annotations:
    metallb.io/address-pool: ip-pool
spec:
  selector:
    app: weerservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
