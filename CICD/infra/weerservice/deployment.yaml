apiVersion: apps/v1
kind: Deployment
metadata:
  name: weer-app
spec:
  replicas: 3 # Draai 3 kopieën voor load-balancing en uptime
  selector:
    matchLabels:
      app: weer
  template:
    metadata:
      labels:
        app: weer
    spec:
      containers:
        - name: weer-app
          image: n64docker/testmet:latest
          ports:
            - containerPort: 80
          # Belangrijk voor uptime:
          readinessProbe:
            httpGet:
              path: /
              port: 80