apiVersion: v2
name: monitoring-stack
description: Full Monitoring Stack (Prometheus + Grafana + Alertmanager)
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: kube-prometheus-stack
    version: 61.3.0   # The specific stable version we want
    repository: https://prometheus-community.github.io/helm-charts
